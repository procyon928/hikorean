<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>안내문 목록</title>
</head>
<body>
    <h1>안내문 목록</h1>
    <a href="/notices/new">새 안내문 작성</a>
    <ul>
        <% notices.forEach(notice => { %>
            <li>
                <a href="/notices/translate/<%= notice._id %>"><%= notice.title %></a> (<%= notice.createdAt.toLocaleDateString() %>)
                <a href="/notices/edit/<%= notice._id %>">수정</a>
                <form action="/notices/delete/<%= notice._id %>" method="POST" style="display:inline;">
                    <button type="submit">삭제</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>
