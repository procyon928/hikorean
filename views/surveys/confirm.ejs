<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>제목</title>
    <meta name="theme-color" content="#254B7A"> <!-- Android Chrome -->
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent"> <!-- iOS Safari -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css"> <!-- CSS 파일 경로 -->
</head>
<body>
    <%- include('../partials/navbar') %> 
    <div class="container-fluid p-0" style="max-width: 960px; margin-bottom: 100px;">
      <div class="content-wrapper p-3">

        <div class="my-3 rounded-3 bg-white p-3">
            <p class="text-center mb-1"><i class="bi bi-check-circle-fill text-success" style="font-size: 30px;"></i></p>
            <p class="text-center text-black fw-bold"><%= texts.survey.responseSubmitted[lang] %></p>
            <p class="text-center"><%= submitResult[lang] || submitResult['ko'] %></p>
        </div>

        <div class="my-3 rounded-3 bg-white p-3">
          <h5 class="fw-bold mt-2 mb-3"><%= texts.survey.submittedResponse[lang] %></h5>
          <% formattedAnswers.forEach((answer, index) => { %>
              <div class="ms-2 mb-3">
                  <p class="fw-bold mb-1"><%= index + 1 %>. <%= questionTitles[index] %></p>
                  <p> : <%= answer.answer || '' %></p>
              </div>
          <% }); %>
        </div>
        
        <div class="text-center">
            <button class="btn btn-primary my-2" onclick="window.history.back();"><%= texts.survey.return[lang] %></button>
        </div>

      </div>
    </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <script>
      document.documentElement.dataset.lang = '<%= lang %>';

      // 사용자가 '뒤로 가기'를 클릭했을 때 새로 고침
      window.onpopstate = function(event) {
          window.location.reload(); // 페이지를 새로 고침
      };

      // 페이지가 언로드될 때 새로 고침을 강제하는 이벤트 리스너 추가
      window.addEventListener('beforeunload', function (event) {
          event.preventDefault(); // 기본 동작 방지
          event.returnValue = ''; // 크롬에서 메시지를 표시할 수 없지만, 이 코드는 필요합니다.
      });

      function redirectToForm() {
          // 폼 페이지로 리다이렉트
          window.location.href = '/surveys/<%= survey._id %>/respond?lang=<%= lang %>';
      }
  </script>
  <script src="/js/lang.js"></script> 
</body>
</html>
